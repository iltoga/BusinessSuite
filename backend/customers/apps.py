"""Django app configuration for the customers module."""

"""Django app configuration for the customers module."""

from django.apps import AppConfig


class CustomersConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "customers"
