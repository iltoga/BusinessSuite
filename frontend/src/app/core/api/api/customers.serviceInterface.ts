/**
 * BusinessSuite API
 *
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
import { HttpHeaders } from '@angular/common/http';

import { Observable } from 'rxjs';

import { Customer } from '../model/models';
import { CustomerRequest } from '../model/models';
import { CustomersBulkDeleteRequestRequest } from '../model/models';
import { CustomersBulkDeleteResponse } from '../model/models';
import { PaginatedCustomerApplicationHistoryList } from '../model/models';
import { PaginatedCustomerList } from '../model/models';
import { PaginatedCustomerUninvoicedApplicationList } from '../model/models';

import { Configuration } from '../configuration';

export interface CustomersApplicationsHistoryListRequestParams {
  id: number;
  ordering?: string;
  page?: number;
  pageSize?: number;
  search?: string;
}

export interface CustomersBulkDeleteCreateRequestParams {
  customersBulkDeleteRequestRequest?: CustomersBulkDeleteRequestRequest;
}

export interface CustomersCheckPassportCreateRequestParams {
  file: Blob;
  method?: string;
}

export interface CustomersCreateRequestParams {
  customerRequest?: CustomerRequest;
}

export interface CustomersDestroyRequestParams {
  id: number;
}

export interface CustomersListRequestParams {
  ordering?: string;
  page?: number;
  pageSize?: number;
  search?: string;
}

export interface CustomersPartialUpdateRequestParams {
  id: number;
  customerRequest?: CustomerRequest;
}

export interface CustomersRetrieveRequestParams {
  id: number;
}

export interface CustomersToggleActiveCreateRequestParams {
  id: number;
  customerRequest?: CustomerRequest;
}

export interface CustomersUninvoicedApplicationsListRequestParams {
  id: number;
  ordering?: string;
  page?: number;
  pageSize?: number;
  search?: string;
}

export interface CustomersUpdateRequestParams {
  id: number;
  customerRequest?: CustomerRequest;
}

export interface CustomersServiceInterface {
  defaultHeaders: HttpHeaders;
  configuration: Configuration;

  /**
   *
   * Shared search configuration for DRF list-style endpoints.  Supports a canonical &#x60;&#x60;?search&#x3D;&#x60;&#x60; param with the legacy &#x60;&#x60;?q&#x3D;&#x60;&#x60; alias, allows opting specific actions into search, and can delegate to a queryset-level custom search method when domain-specific behavior is required.
   * @endpoint get /api/customers/{id}/applications-history/
   * @param requestParameters
   */
  customersApplicationsHistoryList(
    requestParameters: CustomersApplicationsHistoryListRequestParams,
    extraHttpRequestParams?: any,
  ): Observable<PaginatedCustomerApplicationHistoryList>;

  /**
   *
   * Shared search configuration for DRF list-style endpoints.  Supports a canonical &#x60;&#x60;?search&#x3D;&#x60;&#x60; param with the legacy &#x60;&#x60;?q&#x3D;&#x60;&#x60; alias, allows opting specific actions into search, and can delegate to a queryset-level custom search method when domain-specific behavior is required.
   * @endpoint post /api/customers/bulk-delete/
   * @param requestParameters
   */
  customersBulkDeleteCreate(
    requestParameters: CustomersBulkDeleteCreateRequestParams,
    extraHttpRequestParams?: any,
  ): Observable<CustomersBulkDeleteResponse>;

  /**
   *
   * Check passport uploadability asynchronously. Returns an AsyncJob ID to track progress via SSE.
   * @endpoint post /api/customers/check-passport/
   * @param requestParameters
   */
  customersCheckPassportCreate(
    requestParameters: CustomersCheckPassportCreateRequestParams,
    extraHttpRequestParams?: any,
  ): Observable<{}>;

  /**
   *
   * Shared search configuration for DRF list-style endpoints.  Supports a canonical &#x60;&#x60;?search&#x3D;&#x60;&#x60; param with the legacy &#x60;&#x60;?q&#x3D;&#x60;&#x60; alias, allows opting specific actions into search, and can delegate to a queryset-level custom search method when domain-specific behavior is required.
   * @endpoint post /api/customers/
   * @param requestParameters
   */
  customersCreate(
    requestParameters: CustomersCreateRequestParams,
    extraHttpRequestParams?: any,
  ): Observable<Customer>;

  /**
   *
   * Shared search configuration for DRF list-style endpoints.  Supports a canonical &#x60;&#x60;?search&#x3D;&#x60;&#x60; param with the legacy &#x60;&#x60;?q&#x3D;&#x60;&#x60; alias, allows opting specific actions into search, and can delegate to a queryset-level custom search method when domain-specific behavior is required.
   * @endpoint delete /api/customers/{id}/
   * @param requestParameters
   */
  customersDestroy(
    requestParameters: CustomersDestroyRequestParams,
    extraHttpRequestParams?: any,
  ): Observable<{}>;

  /**
   *
   * Shared search configuration for DRF list-style endpoints.  Supports a canonical &#x60;&#x60;?search&#x3D;&#x60;&#x60; param with the legacy &#x60;&#x60;?q&#x3D;&#x60;&#x60; alias, allows opting specific actions into search, and can delegate to a queryset-level custom search method when domain-specific behavior is required.
   * @endpoint get /api/customers/
   * @param requestParameters
   */
  customersList(
    requestParameters: CustomersListRequestParams,
    extraHttpRequestParams?: any,
  ): Observable<PaginatedCustomerList>;

  /**
   *
   * Shared search configuration for DRF list-style endpoints.  Supports a canonical &#x60;&#x60;?search&#x3D;&#x60;&#x60; param with the legacy &#x60;&#x60;?q&#x3D;&#x60;&#x60; alias, allows opting specific actions into search, and can delegate to a queryset-level custom search method when domain-specific behavior is required.
   * @endpoint patch /api/customers/{id}/
   * @param requestParameters
   */
  customersPartialUpdate(
    requestParameters: CustomersPartialUpdateRequestParams,
    extraHttpRequestParams?: any,
  ): Observable<Customer>;

  /**
   *
   * Shared search configuration for DRF list-style endpoints.  Supports a canonical &#x60;&#x60;?search&#x3D;&#x60;&#x60; param with the legacy &#x60;&#x60;?q&#x3D;&#x60;&#x60; alias, allows opting specific actions into search, and can delegate to a queryset-level custom search method when domain-specific behavior is required.
   * @endpoint get /api/customers/{id}/
   * @param requestParameters
   */
  customersRetrieve(
    requestParameters: CustomersRetrieveRequestParams,
    extraHttpRequestParams?: any,
  ): Observable<Customer>;

  /**
   *
   * Shared search configuration for DRF list-style endpoints.  Supports a canonical &#x60;&#x60;?search&#x3D;&#x60;&#x60; param with the legacy &#x60;&#x60;?q&#x3D;&#x60;&#x60; alias, allows opting specific actions into search, and can delegate to a queryset-level custom search method when domain-specific behavior is required.
   * @endpoint get /api/customers/search/
   */
  customersSearchRetrieve(extraHttpRequestParams?: any): Observable<Customer>;

  /**
   *
   * Shared search configuration for DRF list-style endpoints.  Supports a canonical &#x60;&#x60;?search&#x3D;&#x60;&#x60; param with the legacy &#x60;&#x60;?q&#x3D;&#x60;&#x60; alias, allows opting specific actions into search, and can delegate to a queryset-level custom search method when domain-specific behavior is required.
   * @endpoint post /api/customers/{id}/toggle-active/
   * @param requestParameters
   */
  customersToggleActiveCreate(
    requestParameters: CustomersToggleActiveCreateRequestParams,
    extraHttpRequestParams?: any,
  ): Observable<Customer>;

  /**
   *
   * Shared search configuration for DRF list-style endpoints.  Supports a canonical &#x60;&#x60;?search&#x3D;&#x60;&#x60; param with the legacy &#x60;&#x60;?q&#x3D;&#x60;&#x60; alias, allows opting specific actions into search, and can delegate to a queryset-level custom search method when domain-specific behavior is required.
   * @endpoint get /api/customers/{id}/uninvoiced-applications/
   * @param requestParameters
   */
  customersUninvoicedApplicationsList(
    requestParameters: CustomersUninvoicedApplicationsListRequestParams,
    extraHttpRequestParams?: any,
  ): Observable<PaginatedCustomerUninvoicedApplicationList>;

  /**
   *
   * Shared search configuration for DRF list-style endpoints.  Supports a canonical &#x60;&#x60;?search&#x3D;&#x60;&#x60; param with the legacy &#x60;&#x60;?q&#x3D;&#x60;&#x60; alias, allows opting specific actions into search, and can delegate to a queryset-level custom search method when domain-specific behavior is required.
   * @endpoint put /api/customers/{id}/
   * @param requestParameters
   */
  customersUpdate(
    requestParameters: CustomersUpdateRequestParams,
    extraHttpRequestParams?: any,
  ): Observable<Customer>;
}
