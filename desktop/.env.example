# Desktop app startup URL.
# Production default points to your hosted CRM frontend.
DESKTOP_START_URL=https://crm.revisbali.com

# Optional: restrict allowed navigation/notification origin.
# If omitted in runtime, desktop uses DESKTOP_START_URL origin.
DESKTOP_ALLOWED_ORIGIN=https://crm.revisbali.com

# Desktop logging verbosity: debug | info | warn | error
DESKTOP_LOG_LEVEL=info

# Fallback reminder poll interval in milliseconds (default: 15000).
# Used by Electron main process when web push is delayed/unavailable.
DESKTOP_REMINDER_POLL_INTERVAL_MS=15000

# Optional: disable automatic update checks for packaged desktop builds.
# Accepted truthy values: 1, true
DESKTOP_AUTO_UPDATE_DISABLED=0

# Local-first runtime controls.
DESKTOP_LOCAL_FIRST_ENABLED=1
DESKTOP_LOCAL_FRONTEND_URL=http://127.0.0.1:14200
DESKTOP_LOCAL_BACKEND_HEALTH_URL=http://127.0.0.1:18000/api/app-config/
DESKTOP_LOCAL_SYNC_STATE_URL=http://127.0.0.1:18000/api/sync/state/

# Optional explicit compose path for local stack startup.
# DESKTOP_LOCAL_COMPOSE_FILE=/absolute/path/to/docker-compose-desktop-stack.yml

# Optional host port overrides consumed by docker-compose-desktop-stack.yml.
# DESKTOP_STACK_FRONTEND_PORT=14200
# DESKTOP_STACK_BACKEND_PORT=18000

# Desktop-stack specific backend identity/secrets (kept separate from root APP_DOMAIN/SECRET_KEY).
# DESKTOP_APP_DOMAIN=127.0.0.1
# DESKTOP_SECRET_KEY=desktop-local-secret-key-change-me

# Desktop bootstrap admin login user (used by `bun run bootstrap:local`).
# If omitted, fallback is SITE_ADMIN_* from root .env, then hard defaults.
# DESKTOP_SITE_ADMIN_USERNAME=revisadmin
# DESKTOP_SITE_ADMIN_EMAIL=info@example.com
# DESKTOP_SITE_ADMIN_PASSWORD=P12345678!

# Desktop-stack specific DB credentials/host (kept separate from root DB_* variables).
# DESKTOP_DB_HOST=db
# DESKTOP_DB_PORT=5432
# DESKTOP_DB_NAME=business_suite
# DESKTOP_DB_USER=postgres
# DESKTOP_DB_PASS=postgres

# Desktop-stack specific Redis settings.
# DESKTOP_REDIS_HOST=redis
# DESKTOP_REDIS_PORT=6379
# DESKTOP_HUEY_REDIS_DB=0

# Remote sync service token used by desktop local stack.
# Set the same value in VPS and desktop runtime environments.
# DESKTOP_REMOTE_SYNC_TOKEN=replace-with-long-random-token
# Note: you can also just use LOCAL_SYNC_REMOTE_TOKEN for both purposes if you don't need separate tokens for local stack and VPS.
